<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_marginLeft="20dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">
    <TextView
        android:visibility="gone"
        android:id="@+id/horizontal_gallery_item_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"/>
    <ImageView
        android:id="@+id/horizontal_gallery_item_image"
        android:layout_above="@id/horizontal_gallery_item_text"
        android:scaleType="fitCenter"
        android:layout_width="100dp"
        android:layout_height="wrap_content" />
    <ImageView
        android:id="@+id/horizontal_gallery_item_remove"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/icon_remove"
        android:layout_alignTop="@id/horizontal_gallery_item_image"
        android:layout_alignRight="@id/horizontal_gallery_item_image" />

</RelativeLayout>